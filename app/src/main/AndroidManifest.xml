<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chloechoi.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.main.WorkboxActivity"/>

        <activity android:name=".activities.main.fragment.SettingFragment">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>



        <activity android:name=".activities.main.fragment.SettingProfileFragment"/>
        <activity android:name=".activities.main.fragment.SettingProfileModifyFragment"/>
        <activity android:name=".activities.main.fragment.SettingThemeFragment"/>

        <!--<activity android:name=".activities.main.WorkboxActivity"></activity>-->
        <activity android:name=".activities.main.TodoAddPickerActivity"/>
        <activity android:name=".activities.SignInActivity"/>

    </application>

</manifest>